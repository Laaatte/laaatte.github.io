# Data

This directory is intentionally empty before build.

Files in this directory are generated at build time
(e.g. JSON data used by client-side scripts).

Do not remove this directory.
Do not commit generated files unless explicitly required.